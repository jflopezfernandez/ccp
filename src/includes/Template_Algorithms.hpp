#ifndef _TEMPLATE_ALGORITHMS_HPP
#define _TEMPLATE_ALGORITHMS_HPP


#include "Template_Algorithms/TA_Utils.hpp"

/*
template <typename T>
void sort(T *t) {
	T temp;
	
	for (int i = 0; i < size + 1; ++i) {
		for (int j = 0; j < size - 1; ++j) {
			if (t[j] > t[j+1]) {
				temp = t[j];
				t[j] = t[j+1];
				t[j+1] = temp;
				
				printArray(t);
			}
		}
	}
}
*/


#endif /* _TEMPLATE_ALGORITHMS_HPP */